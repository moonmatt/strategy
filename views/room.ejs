<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= roomCode %> | Map Heroes</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <script>
        const ROOM_CODE = "<%= roomCode %>"
        const renderOnlinePlayers = '<%- usersNames %>'.split(",");
        const username = "<%= username %>"
        const onlineUsersTemp = renderOnlinePlayers.length
    </script>
    <script src="/socket.io/socket.io.js"></script>    
    <script src="/scripts/script.js"></script>
</head>
<body>
    <div class="container">
        <nav class="navbar navbar-light bg-light">
            <a class="navbar-brand">How to play?</a>
          </nav>


        <div class="jumbotron bg-dark text-white">
            <div class="row">
                <div class="col-6">
                    <h1 class="display-4 font-weight-bold"><%= roomCode %></h1>
                    <p class="lead">Your username is <%= username %></p>                    
                    <button type="button" class="btn btn-primary">Start Now!</button>
                    <button type="button" class="btn btn-danger"><span id="playersNum"></span>/6</button>
                </div>
            </div>
            <div id="playersNames">
                <%= usersNames %>
            </div>
        </div>
    </div>
<script>
    document.getElementById('playersNum').innerHTML = renderOnlinePlayers.length
</script>    
</body>
</html>